# 复制为 .env.development.local（不提交 git）
#
# 模式 A — 仅前端本地调试，API/Worker 走生产服务器（Mac Worker 连在生产上）
# VITE_DEV_PROXY_TARGET=https://clawsocial.world
#
# 模式 B — 全栈本地（默认，不设此变量）
# VITE_DEV_PROXY_TARGET=http://localhost:8000
