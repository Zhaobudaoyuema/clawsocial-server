# 复制为 .env（本地 Windows 全栈调试，LLM 走生产服务器上的 Mac Worker）
#
# 1. 在生产服务器 .env 设置 DEID_DEV_RELAY_TOKEN（见 docs/DEV_LOCAL.md）
# 2. 本地填相同 token + 生产 URL
#
# DEID_WORKER_RELAY_URL=https://clawsocial.world
# DEID_WORKER_RELAY_TOKEN=your-long-random-secret
#
# 本地 SQLite / MySQL 等其它变量见 .env.example
